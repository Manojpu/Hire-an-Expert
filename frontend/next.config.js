/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  output: "standalone",
  // Add other configurations like images domains if needed
};

module.exports = nextConfig;
